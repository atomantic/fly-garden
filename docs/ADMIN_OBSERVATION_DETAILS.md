# Read-only admin observation details

The fixture event journal now opens the exact captured individual/session event and its simulation-time window. It includes the retained controller-frame trace and garden transitions as separately labeled source records. Related artifact actions require the same individual/session and exact simulation time; notes/marks additionally require the declared source action ID. Language links require the same recipient/session and an evidence window containing that time. Temporal coincidence is not labeled causation. Missing, discarded or unsupported sources remain explicitly unavailable; no historical neural sample is reconstructed from current values.

An explicit event selection performs only bounded existing GET reads for retained artifact/language records plus a recipient-session confirmation. It does not restore recordings, call providers, offer inputs, load workers or start time. Scope/generation changes invalidate delayed replies. Detailed records remain labeled historical captured data and identify their source window; later activity does not silently update them. Standard read-only recordings remain the source for exact historical neuron values.

The fixture mean-rate chart retains at most 50 distinct simulation timestamps. Repeated paused polls add no points, backward responses are ignored, and another individual or restored session resets the sequence. Horizontal position uses simulation milliseconds rather than polling ordinal. The displayed mean is over 32 synthetic neurons with the fixed trailing 1000 ms rate window, including the existing initial zero-padding convention. Missing replies add no zero samples. A restored session never displays the prior session's line.

The connectome lab labels potentials dimensionless and exposes age since the last accepted browser receipt. This is not worker-sample age and clock rollback yields unavailable. Disconnected/read failures retain a stale warning. Speed remains explicitly unavailable because no timed batch measurement is supplied; polling cadence is never claimed as throughput. Reported finite bounds are a numerical observation, not a validated biological welfare metric.

These changes complete administrative inspection of currently supported state. No learned-weight baseline is fabricated: current fixture/research model weights remain fixed engineered mappings pinned to their graph/model provenance. Full-connectome embodiment, retained learning and spatial fallback remain separate work. Keyboard-native buttons/details, wrapping record text and the existing reduced-motion rules apply; no new animation is introduced.

Validation uses pure timestamp/correlation tests, existing source-guard tests and a build to an isolated temporary output directory. It runs no neural campaign, provider or production operation.
