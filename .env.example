# Default is loopback. For access from your private Tailscale network:
# HOST=0.0.0.0
# ALLOWED_HOSTS=your-machine.your-tailnet.ts.net
# Exact comma-separated hostnames, without scheme or port. Loopback stays allowed.
